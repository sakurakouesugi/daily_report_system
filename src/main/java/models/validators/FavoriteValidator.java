package models.validators;

//public class FavoriteValidator {

   //  public static List<String> validate(
          //  FavoriteService service, FavoriteView fv, Boolean codeDuplicateCheckFlag) {
       // List<String> errors = new ArrayList<String>();


      //TODO   String codeError = validateCode(service, fv.getEmployee(), codeDuplicateCheckFlag);
        //if (!codeError.equals("")) {
          //  errors.add(codeError);

   // return errors;

